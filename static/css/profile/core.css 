/* static/css/profile/core.css */

/* Core styles */
body {
    background-color: #121212;
    color: white;
    font-family: 'League Spartan', sans-serif;
}

/* Neural network background */
.neural-bg {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: 
        radial-gradient(circle at 20% 30%, rgba(0, 159, 220, 0.05) 0%, transparent 50%),
        radial-gradient(circle at 80% 70%, rgba(0, 159, 220, 0.05) 0%, transparent 50%);
    z-index: -1;
    opacity: 0.5;
}

/* Common styles */
.rounded-full {
    border-radius: 9999px;
}

.text-gradient {
    background: linear-gradient(90deg, #009fdc, #00c3dc);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
}

/* Container styles */
.container {
    max-width: 1280px;
    margin: 0 auto;
    padding: 2rem 1rem;
}

/* Card styles */
.card {
    background: rgba(30, 30, 30, 0.9);
    border: 1px solid rgba(0, 159, 220, 0.3);
    border-radius: 16px;
    padding: 2rem;
    margin-bottom: 2rem;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
    backdrop-filter: blur(8px);
}

/* Progress bar base styles */
.progress-bar {
    height: 8px;
    background: rgba(0, 159, 220, 0.2);
    border-radius: 4px;
    overflow: hidden;
}

/* Typography */
h1, h2, h3, h4, h5, h6 {
    font-weight: bold;
    line-height: 1.2;
}

.text-primary {
    color: #009fdc;
}

.text-secondary {
    color: rgba(255, 255, 255, 0.7);
}

/* Grid layouts */
.grid-layout {
    display: grid;
    gap: 1.5rem;
}

/* Responsive breakpoints */
@media (max-width: 768px) {
    .container {
        padding: 1rem;
    }
}

@media (max-width: 640px) {
    h1 {
        font-size: 1.875rem;
    }
}